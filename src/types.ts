/**
 * Public TypeScript declaration entrypoint for `@swarmmachina/benchkit`.
 *
 * Runtime imports continue to resolve through `index.js`. This module gives
 * package managers and TypeScript a stable root `types.d.ts` artifact while
 * preserving domain-owned declarations and their JSDoc.
 */
export * from './index.js'
